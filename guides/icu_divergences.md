# ICU Divergences

Localize is tested against conformance data that Unicode generates by running **ICU**. Most of the time the two agree and a mismatch means Localize has a defect. Sometimes they do not agree, because ICU is an implementation with its own API surface and defaults, not the specification itself. This guide records where Localize deliberately differs from ICU, why, and how each case can be checked.

## The rule

**Where the format CLDR ships and ICU's behaviour conflict, Localize follows CLDR.**

CLDR is the data. ICU is one consumer of it, and a conformance file generated from ICU records ICU's answer — including where ICU overrides, ignores, or supplements what the data says. Localize renders what the locale's data specifies, so a locale maintainer who changes a pattern in CLDR sees that change in Localize's output.

This is not a claim that ICU is wrong. In several cases below ICU's output is arguably the friendlier one. It is a statement about which source is authoritative when they disagree, so that the answer is predictable rather than decided case by case.

## Distinguishing a divergence from a defect

Four different things can make a conformance case fail, and only the first is a divergence:

* **Divergence** — CLDR's data says one thing, ICU does another, and Localize follows CLDR. Recorded, asserted, and never "fixed".
* **Contract mismatch** — the fixture was generated through an ICU API whose defaults differ from the CLDR pattern's. Not a divergence: the test has to ask the same question ICU was asked.
* **Upstream fixture issue** — the conformance file itself disagrees with CLDR's own data.
* **Defect** — Localize is wrong. Fix it.

The distinction matters because they are easy to confuse, and mislabelling a defect as a divergence hides a bug behind a comment. Before recording anything as a divergence, read the locale's data and confirm that Localize is rendering what CLDR ships.

### Contract mismatch, in practice

CLDR 49's decimal conformance suite is generated by `org.unicode.cldr.tool.GenerateDecimalFormatTestData`, which builds an ICU `NumberFormatter` with **no explicit precision**. `NumberFormatter`'s default is up to six fraction digits, where the CLDR decimal pattern `#,##0.###` allows three. Run the fixtures against the pattern default and thousands of cases fail — none of them because the formatting is wrong.

`test/localize/number/decimal_conformance_test.exs` therefore passes `max_fractional_digits: 6` for the standard dimensions, matching the generator. This is why the generator source is worth reading before wiring up any new conformance suite: the fixture encodes the API call that produced it, not just the expected strings.

## Recorded divergences

### Number formatting

Enumerated in `@divergences` in `test/localize/number/decimal_conformance_test.exs`, which asserts the **count** of each rather than merely skipping them — a divergence that resolves upstream, or one that grows, moves the number and the test reports it.

| Locales | Dimension | CLDR ships | ICU renders |
|---|---|---|---|
| `gu`, `hi`, `hi-Latn`, `lo`, `mr`, `pa`, `si` | scientific | the pattern `[#E0]`, brackets included | ignores the locale pattern and builds the notation itself |
| `ur` | percent | `symbols-numberSystem-latn.percentSign` is `٪` (U+066A) | `%`, because the generator formats percent as a measure unit and `ur`'s unit pattern is `{0}%` |
| `gl` | decimal, percent | group separator U+00A0 | `.` |
| `bn` | compact long | `00000 কোটি`, carrying no grouping | applies the locale's standard grouping to the mantissa |
| `af` | compact short | `0 m'.'`, whose quoted literal is a full stop | drops the full stop |
| `en-IN`, `hi-Latn` | compact long | `0 million` | `12 lakh` |
| `ig`, `ps`, `ta` | compact | localised patterns `0 nde`, `0 میلیونه`, `0M` | root forms, or no compact form at all |

The `ur` case is the subtlest: both `٪` and `%` come from CLDR, one from the number symbols and one from the unit data. Localize formats percent through the number pattern, so it uses the symbol; the generator formats it as a measure unit, so it uses the unit pattern.

### Where ICU4C is used directly

`Localize.Nif.number_format/3` is ICU4C's `NumberFormatter` and therefore produces ICU's answers, not Localize's, for every case in the table above. The NIF is built for cross-validation and for collation sort keys, not as a drop-in for `Localize.Number.to_string/2`; the two are not expected to agree in these locales. See the NIF section of the [number formatting guide](number_formatting.md).

## Not divergences

These also appear as exclusions in the test suites and should not be read as deliberate differences:

* **Unimplemented features.** `test/localize/locale/locale_display_test.exs` excludes line 47, the `uu` attribute in the `-u-` extension. That is a gap, not a decision.
* **Timezone and skeleton gaps.** `test/localize/datetime/conformance_test.exs` excludes cases needing timezone name resolution (`z`, `Z`, `O`, `v`, `V`) and some skeleton matching. Also gaps.
* **Upstream fixture issues.** The same file's `@invalid_test_results` and the locale-display suite's `@invalid_test_results` mark cases where the conformance data disagrees with CLDR's own data.

Collation carries no exclusions at all: all 210,155 pairs in both CLDR conformance files pass under both strengths.

## Adding a divergence

1. **Read the locale's data.** Open the relevant file under `priv/cldr/locales/<locale>/` and find the pattern or symbol in question. If Localize is not rendering what is there, it is a defect — stop and fix it.
2. **Check what produced the fixture.** Find the generator in `$CLDR_REPO/tools/` and read the API call. A contract mismatch is fixed in the test, not recorded as a divergence.
3. **Record it with its evidence.** Name what CLDR ships and what ICU renders, so the entry can be re-checked without repeating the investigation.
4. **Assert the count, not just the membership.** An exclusion that silently absorbs new failures stops being a record and becomes a blind spot.
